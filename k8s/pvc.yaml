apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .name | defailt 'my-pvc' }}
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: {{ .size | default '100Mi' }}
  storageClassName: {{ .sc-name | default }}
~          
